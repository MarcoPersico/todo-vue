import Vue from 'vue'
import App from './App.vue'

import '../assets/app.scss'

// eslint-disable-next-line no-new
new Vue({
  el: '#app',
  data: () => { return {} },
  render: h => h(App)
})
